User json
    name Text
    grade Text
    chipId Text -- Base64?
    login Text Maybe
    password Text Maybe
    access [Access]
Access json
    time UTCTime
    inside Bool
    deriving (Eq)
Vote -- TODO: maybe add json (getVoteInfoR)
    identity Int
    description Text
    voted [UserId]
    choices [Choice]
    deriving (Show) -- TODO: Debug. Remove
Choice
    identity Int
    votes Int
    description Text
    deriving (Show)
