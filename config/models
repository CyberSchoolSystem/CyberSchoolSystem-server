-- TODO: Research MongoDB Document IDs. R they enough
-- TODO: How should I save border actions?
User
    name Text
    class Text
    chipId Text -- Base64?
    login Text Maybe
    password Text Maybe
    deriving (Show)
Choice json
    identity Int
    votes Int
    description Text
    deriving (Show)
Vote json
    identity Int
    description Text
    voted [UserId]
    choices [Choice]
    deriving (Show) -- TODO: Debug. Remove
