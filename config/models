User json
    name Text
    grade Text
    chipId Text -- Base64?
    login Text Maybe
    password Text Maybe
    access [Access]
Access json
    time UTCTime
    inside Bool
    deriving (Eq)
Choice json
    identity Int
    votes Int
    description Text
    deriving (Show)
Vote json
    identity Int
    description Text
    voted [UserId]
    choices [Choice]
    deriving (Show) -- TODO: Debug. Remove
